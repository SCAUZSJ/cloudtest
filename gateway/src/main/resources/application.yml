server:
  port: 9099

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://milo:123456@milo-eureka:7000/eureka/


spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        - id: order  #???????????id
          uri: lb://order-service  # ??URL?lb????????????lb?Load Balance???
          predicates:
            # Predicate??
            - Path=/api/order/**  # ????
          filters:
            # Filter??
            - StripPrefix=1  # ?????????
        - id: book
          uri: lb://book-service
          predicates:
            - Path=/api/book/**
          filters:
            # Filter??
            - StripPrefix=1  # ?????????


